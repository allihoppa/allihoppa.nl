version: "2"

services:
  app:
    extends:
      file: ../default/docker-compose.yml
      service: app
    container_name: allihoppa-nl-app-ci
    build:
      context: ../../docker/service/app/dist
